# Web
# Empty/relative = same-origin (Next.js rewrites proxy /api/v1/* to the API).
# Set explicitly when the web app and API are deployed on separate origins:
# NEXT_PUBLIC_API_URL=https://api.example.com/api/v1
NEXT_PUBLIC_API_URL=
