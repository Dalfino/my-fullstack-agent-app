services:
  postgres:
    image: pgvector/pgvector:pg16
    container_name: ts-postgres
    environment:
      POSTGRES_USER: talentshowcase
      POSTGRES_PASSWORD: talentshowcase
      POSTGRES_DB: talentshowcase
    ports:
      - '5432:5432'
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U talentshowcase']
      interval: 5s
      timeout: 5s
      retries: 10

  redis:
    image: redis:7-alpine
    container_name: ts-redis
    ports:
      - '6379:6379'
    volumes:
      - redis_data:/data
    healthcheck:
      test: ['CMD', 'redis-cli', 'ping']
      interval: 5s
      timeout: 5s
      retries: 10

  minio:
    image: minio/minio:latest
    container_name: ts-minio
    command: server /data --console-address ':9001'
    environment:
      MINIO_ROOT_USER: talentshowcase
      MINIO_ROOT_PASSWORD: talentshowcase
    ports:
      - '9000:9000'
      - '9001:9001'
    volumes:
      - minio_data:/data
    healthcheck:
      test: ['CMD', 'mc', 'ready', 'local']
      interval: 5s
      timeout: 5s
      retries: 10

  rabbitmq:
    image: rabbitmq:3.13-management
    container_name: ts-rabbitmq
    environment:
      RABBITMQ_DEFAULT_USER: talentshowcase
      RABBITMQ_DEFAULT_PASS: talentshowcase
    ports:
      - '5672:5672'
      - '15672:15672'
    volumes:
      - rabbitmq_data:/var/lib/rabbitmq
    healthcheck:
      test: ['CMD', 'rabbitmq-diagnostics', '-q', 'ping']
      interval: 10s
      timeout: 10s
      retries: 10

volumes:
  postgres_data:
  redis_data:
  minio_data:
  rabbitmq_data:
